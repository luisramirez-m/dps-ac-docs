# ¿Como commentar en Git?

## Add

Esta `etiqueta` se usa para agregar cosas nuevas como; `features`, nuevo codigo, nuevas carpetas, etc.

```bash
add: new component user
```

### Ejemplo real

```bash
git commit -m "add: new component user"
```